import React from 'react'

const DisplayGrid = ({deviceName}) => {
    return (
        <div>
            {deviceName.map(element => {
                <p></p>
            })}
            
        </div>
    )
}

export default DisplayGrid
